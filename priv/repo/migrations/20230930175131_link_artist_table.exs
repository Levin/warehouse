defmodule Warehouse.Repo.Migrations.LinkArtistTable do
  use Ecto.Migration

  def change do

    #alter table("auctions") do
    #  add :auction_id, references(:auctions, type: :integer, on_delete: :delete_all), null: true
    #end
  end
end
