defmodule Warehouse.Repo.Migrations.AlterTableAuctions do
  use Ecto.Migration

  def change do

    #alter table(:auctions) do
    #  add :artist_id, references(:artists, type: :integer, on_delete: :delete_all), null: true
    #end

  end
end
